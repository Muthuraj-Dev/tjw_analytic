enum ViewState { Idle, Busy }

enum SectionType { article, updates, liveRate }

enum BranchCode {
  unpaid,
  pending,
  partial,
  paid,
  overdue,
  refunded,
  failed,
  cancelled,
  disputed,
}

enum Gender {
  male,
  female,
  other
}

enum IDType {
  aadhaar,
  panCard,
  passport,
  drivingLicense,
  voterId,
  rationCard,
  other,
}

enum CompanyType {
  HUF,
  LLP,
  Partnership,
  Proprietorship,
  Public_Ltd,
  Pvt_Ltd,
}
